{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 && ./flutter/bin/flutter build web",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}